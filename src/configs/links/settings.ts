const path = '/settings';

const USER = {
    path: 'user',
    fullPath: `${path}/user`,
}

const VISUAL = {
    path: 'visual',
    fullPath: `${path}/visual`,
}

export const SETTINGS = {
    path,
    USER,
    VISUAL,
};
